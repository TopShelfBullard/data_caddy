<!DOCTYPE html>
<html>
<head>
  <title>Data Caddy</title>
  <meta name="viewport" content="width=device-width">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">
  <div class="row">
    <div class="col col-xs-10 col-xs-offset-1">

      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>

      <p class="navbar-text pull-right">
        <% if player_signed_in? %>
          Logged in as <strong><%= current_player.name %></strong>.
          <%= link_to "My Rounds", "/rounds" %> |
          <%= link_to "Edit profile", edit_player_registration_path, :class => "navbar-link" %> |
          <%= link_to "Logout", destroy_player_session_path, method: :delete, :class => "navbar-link"  %>
        <% else %>
          <%= link_to "Sign up", new_player_registration_path, :class => "navbar-link"  %> |
          <%= link_to "Login", new_player_session_path, :class => "navbar-link"  %>
        <% end %>
      </p>

      <%= yield %>

      </div>
  </div>
</div>

</body>
</html>
